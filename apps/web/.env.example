API_URL=http://localhost:8080

# Optional self-hosted Umami tracker. Analytics stays disabled unless all three
# values are valid; the domain allowlist prevents preview traffic collection.
UMAMI_SCRIPT_URL=
UMAMI_WEBSITE_ID=
UMAMI_DOMAINS=
