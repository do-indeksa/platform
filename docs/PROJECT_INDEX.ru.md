# Do indeksa: индекс проекта и продуктовая правда

Срез состояния: 11 августа 2026 года.

Этот документ связывает продуктовые документы, исследования FTN, дизайн и
фактическую реализацию. Он не заменяет первоисточники. Его задача - дать одну
точку входа и явно отметить, что является подтвержденным фактом, продуктовым
решением, дизайнерским примером или текущим ограничением кода.

## 1. Короткое определение продукта

**Do indeksa** - бесплатный трехъязычный тренажер для подготовки к
математическому вступительному экзамену FTN P1, который измеряет текущую
готовность ученика и каждый раз предлагает следующий конкретный шаг. Канонический
экзаменационный контент создается на сербской латинице; интерфейс и переводы
поддерживают `sr-Latn`, `en` и `ru`.

Основной цикл MVP:

1. Решить задачу или пройти диагностику.
2. Получить точную обратную связь, разбор и типичную ошибку.
3. Обновить карту знаний и зависимость результата от подсказок.
4. Получить следующую короткую тренировку по слабым областям.
5. Периодически пройти полный пробный P1 и пересчитать план.

Справочник факультетов и калькулятор поступления - второй продуктовый контур.
Он полезен, но не должен задерживать доказательство основного цикла подготовки.

## 2. Иерархия источников

При конфликте фактов использовать следующий порядок:

1. Актуальные официальные страницы и документы FTN.
2. Официальные варианты и решения экзаменов конкретного года.
3. Локальное исследование исторических экзаменов и авторские материалы.
4. `docs/PRODUCT.md`, ADR и другие утвержденные документы репозитория.

Источники дизайна имеют отдельные границы: PDF задает исходную UX-логику, а
конкретный Figma node является обязательной pixel-perfect спецификацией
композиции, responsive states и визуального слоя. Текущий код описывает только
реализованное состояние и не переопределяет ни продукт, ни дизайн.

Тексты, числа и категории в PDF/Figma являются демонстрационными данными, пока
они не подтверждены источниками уровней 1-4. Это позволяет заменить ложные
данные, но не позволяет самовольно менять Figma-композицию.

## 3. Подтвержденные факты FTN P1

Актуальная официальная страница FTN указывает для экзамена P1 по математике:

- 10 задач;
- 4 часа;
- максимум 60 баллов;
- готовые варианты ответа не предлагаются;
- оценивается ход решения, поэтому возможны частичные баллы за незавершенную
  задачу.

Источники:

- [Način polaganja](https://ftn.uns.ac.rs/nacin-polaganja/)
- [Pet želja: группы программ по экзаменам](https://ftn.uns.ac.rs/upis/pet-zelja/)
- [Oblasti iz matematike](https://ftn.uns.ac.rs/oblasti-iz-matematike/)
- [Официальные материалы и решения](https://ftn.uns.ac.rs/zbirka-zadataka-i-literatura/)
- [Решения P1 от 30 июня 2026 года](https://prijemni.ftn.uns.ac.rs/php/getResenjaTesta.php?VRT_SIFRA=P1)

Следствия для продукта:

- зафиксированные в старых документах и коде 180 минут больше не являются
  актуальным официальным форматом;
- булево `correct` и формула `correct ? 6 : 0` не воспроизводят официальное
  оценивание;
- автоматическая проверка конечного ответа полезна в тренировке, но сама по
  себе не может называться точным экзаменационным баллом;
- в полном пробнике нужен пошаговый rubric/self-check с частичными баллами или
  явно маркированная "оценка тренажера".

### Актуальная карта экзаменов FTN

Официальная структура приема 2026 года использует следующие обозначения:

| Код | Экзамен                                                    |
| --- | ---------------------------------------------------------- |
| P1  | математика: 10 задач, 4 часа, 60 баллов                    |
| P3  | математика с логикой: 5 задач и 15 вопросов по логике      |
| P4  | математика с проверкой склонностей для GRID                |
| P5  | математика с проверкой склонностей для охраны среды/труда  |
| P6  | математика с проверкой склонностей для строительства       |
| P7  | комплексный тест для архитектуры                           |
| P8  | общая культура и собеседование для сценической архитектуры |

В актуальной структуре нет действующего `P2` и нет отдельного вступительного
экзамена по физике. `P3` не является следующим уровнем `P1`: это другой экзамен
и другая группа программ. P3-P8 могут появиться в справочнике поступления, но не
в фильтрах тренажера P1, пока для них нет отдельного проверенного продукта.

### Темы и позиции

Исторический каркас P1 устойчив на уровне областей:

| Позиция | Рабочая область                                 |
| ------- | ----------------------------------------------- |
| 1       | комплексные числа                               |
| 2       | квадратные уравнения и формулы Виета            |
| 3-4     | логарифмические и экспоненциальные задачи       |
| 5       | тригонометрия                                   |
| 6       | векторы и аналитическая геометрия               |
| 7       | планиметрия                                     |
| 8       | стереометрия                                    |
| 9       | анализ функции, производная, пределы и интеграл |
| 10      | комбинаторика                                   |

Позицию нельзя навсегда отождествлять с одной темой. В варианте 2025 года на
позиции 3 были логарифмы, а на позиции 4 - экспонента; в официальном P1 2026
года эти две области поменялись местами. Поэтому в контентной модели нужны
отдельные поля `topic` и `eligiblePositions`/`examPosition`, а вариант должен
собираться по версионированному blueprint конкретного формата.

## 4. Карта источников

### Репозиторий

- `CLAUDE.md` - правила проекта, язык, стек, copyright и локальные источники.
- `docs/PRODUCT.md` - целевая аудитория, MVP и основной учебный цикл.
- `docs/ENGINEERING.md` - границы frontend/backend и правила состояния.
- `docs/decisions/` - принятые архитектурные решения.
- `docs/research/global-exam-prep-platforms-2026.ru.md` - конкурентный анализ и
  рекомендуемый P0/P1.
- `content/topics.yaml` - текущая таксономия контента.
- `content/tasks/` - 30 авторских задач: по 3 на каждую из 10 областей. Все
  10 полных тем (30 задач) имеют статус `verified` и versioned review-record.
- `content/snapshots/tasks/` - append-only архив точных verified Markdown
  revisions, адресованных task ID и SHA-256.
- `content/sources/` и `tools/content/` - hash-pinned авторские LaTeX-источники,
  AST-импорт, проверка provenance/snapshots и запрет автоматического статуса
  `verified`.
- `content/guide/ftn/cutoffs.yaml` - данные калькулятора проходных баллов.
- `apps/web/` - Next.js 16, React 19, next-intl, KaTeX, mathjs, Zustand.
- `apps/api/` - Go/chi API, Google OAuth, сессии и журнал попыток в Postgres.

### Локальные FTN-материалы

Эти каталоги являются исследовательскими источниками, а не публикуемым
контентом:

- `~/Documents/FTN/FTN_P1_Materijali/README.md`
- `~/Documents/FTN/FTN_P1_Materijali/_Reference/FTN_P1_Spec_i_Pokrytie.md`
- `~/Documents/FTN/FTN_P1_Materijali/_Reference/FTN_P1_Checklist_Tem.md`
- `~/Documents/FTN/FTN_P1_Materijali/01_Originali_FTN_P1_2012-2025/`
- `~/Documents/FTN/FTN_P1_Materijali/07_Autorske_probne_varijante/`
- `~/Documents/FTN/Prijem_i_napravljenja/FTN_napravlenija.md`
- `~/Documents/FTN/Prijem_i_napravljenja/FTN_prohodni_analitika.md`
- `~/Documents/FTN/Prijem_i_napravljenja/FTN_site_map.md`

Оригинальные PDF FTN, OCR книги на 688 задач и материалы ETF нельзя коммитить.
В репозиторий попадают собственные задачи, собственные метаданные и ссылки на
официальные источники.

### Дизайн

- Локальная PDF-спецификация:
  `~/Downloads/do-indeksa-design-spec-ru-layered.pdf`, 45 страниц.
- [Figma: Do indeksa - UX/UI demo](https://www.figma.com/design/d7BeJCrce0Z1kedvphZQJo/Do-indeksa-%E2%80%94-UX-UI-%D0%B4%D0%B5%D0%BC%D0%BE?node-id=195-617)
- [Pixel-perfect контракт дизайна](DESIGN_CONTRACT.ru.md) - точные Figma nodes,
  обязательные правила реализации и допустимые продуктовые замены.

В Figma проработаны landing, кабинет, решение задачи, банк задач, план,
конструктор тренировки и история для desktop/tablet/mobile. Есть варианты RU,
EN и SR, гостевые и авторизованные состояния.

## 5. Что уже реализовано

| Возможность              | Состояние              | Комментарий                                                               |
| ------------------------ | ---------------------- | ------------------------------------------------------------------------- |
| Google OAuth и сессия    | код готов              | PKCE, sealed state, secure cookie, preview exchange; production key нужен |
| Банк задач               | готов для preview      | поиск, фильтры, выбор набора, 30 задач в 10 областях                      |
| Решение отдельной задачи | готов для preview      | exact checker, подсказки, решение, resume, история и task-report          |
| Журнал попыток           | готов для preview      | rich local-first GraphQL sync, idempotent UUID и безопасный guest claim   |
| Карта знаний             | рабочая эвристика      | локальная оценка по недавним попыткам, требует pilot-калибровки           |
| Диагностика              | готова для preview     | resumable набор из 10 позиций, результат без обещания официального балла  |
| Пробный экзамен          | готов для preview      | 4 часа, cloud resume, AUTO + rubric self-check, partial score и разбор    |
| История                  | готова для preview     | sync, фильтры, immutable task/mock detail и честный score trend полных P1 |
| Персональный план        | упрощенный MVP         | deterministic checklist, настройки даты/темпа, resume                     |
| Конструктор тренировки   | упрощенный MVP         | bounded balanced set и выбранные вручную задачи                           |
| Справочник факультетов   | актуальный FTN-каталог | 29 программ, официальные группы P1/P3-P8, поиск и cutoff-калькулятор      |
| Локализация и app shell  | готовы                 | `sr-Latn`/`en`/`ru`, responsive desktop/tablet/mobile                     |
| Контентный pipeline      | готов                  | 30 provenance-ссылок, 30 verified-задач и 31 immutable revisions          |
| Production deploy        | ожидает OAuth и Tunnel | `doindeksa.rs`, private origin; analytics остается fail-closed            |

Текущие маршруты web:

`/`, `/prep`, `/tasks`, `/tasks/[topic]`, `/tasks/[topic]/[id]`,
`/diagnostic`, `/diagnostic/new`, `/diagnostic/result`, `/simulation`,
`/simulation/new`, `/simulation/result`, `/history`, `/exams`,
`/exams/[examId]`, `/faculties/ftn`, `/calculator` во всех трех locale.

HTTP API сохраняет `/v1/auth/*`, `/v1/me` и совместимый `/v1/attempts` только
для дренирования старых локальных записей. Новые попытки, история, диагностика
и пробный экзамен используют product API на `/graphql`: `Run`, `RunItem`,
расширенный `Attempt`, start/record/checkpoint/submit/abandon lifecycle, recent
history и bounded-проекцию `completedSimulationRuns` без N+1.

## 6. Разбор PDF и Figma

### Что воспроизвести по Figma

- точную композицию, размеры, typography, spacing, colors и assets каждого
  существующего screen node;
- responsive Marketing/App Header и mobile composition из соответствующих
  Figma variants, без самостоятельно добавленной нижней навигации;
- сценарии guest-first и регистрацию после первой полученной ценности;
- состояния банка задач, пустые состояния, сохранение и продолжение;
- кабинет с одним главным действием "продолжить";
- структуру плана по областям и понятную историю попыток;
- запрет подсказок и мгновенной проверки в экзаменационном режиме.

### Что заменить до реализации

- общую российскую школьную таксономию в позициях 1-10 на модель FTN P1;
- Physics и P2 удалить как несуществующие в актуальной структуре, а P3 не
  показывать как уровень или продолжение P1;
- фальшивые объемы `2 345`/`1 876` задач на фактические значения;
- баллы пробников `62`, `65`, `78`: P1 ограничен 60 баллами;
- цель `8+ баллов`: нужно различать `8/10 задач` и экзаменационные баллы;
- устаревшие даты и расчетные часы без подтвержденного алгоритма;
- русский UI как единственный production-default: поддерживаются `sr-Latn`,
  `en` и `ru`, при этом сербский является каноническим языком контента;
- широкий multi-university promise в первом экране до готовности FTN P1.

Landing должен сначала доказать конкретный продукт: FTN P1, 10 задач, 4 часа,
60 баллов, живой пример задачи, мгновенная тренировочная проверка и понятный
следующий шаг. Остальные экзамены FTN могут появиться в справочнике, но не
являются обещанием тренировочного MVP.

## 7. Оставшиеся доменные разрывы

### Task

Текущий Markdown уже разделяет тему, позицию, источник, сложность, статус,
machine-check, подсказки и решение. Для полного P1 еще нужны:

- `eligiblePositions` отдельно от текущего одиночного `slot`;
- структурированные source/year/url вместо одной строки;
- rubric и максимальные баллы по частям;
- типичная ошибка и связи с похожими задачами.

### Attempt

GraphQL/Postgres и web sync хранят стабильные ID, run item или standalone task,
ответ, время, outcome, help level, grading kind, earned/max points и task
revision. Гостевая очередь атомарно получает owner при входе, повторы используют
тот же UUID, а данные другого аккаунта не попадают в активное представление.
History UI показывает ответы, длительность, способ проверки, баллы, revision и
все outcomes; локальные legacy-детали и архив пробников изолированы по owner.
Завершённые пробники объединяются с серверным архивом по run UUID, поэтому их
можно открыть на другом устройстве. Точные verified Markdown snapshots условий
и решений сохраняются по task ID и revision. Ссылка детали передаёт только
публичную SHA-256 revision; web resolver перепроверяет ID, bytes и metadata общим
parser, а клиент выбирает архив только после точного совпадения с owner-scoped
hydrated entry. Для записанной, но отсутствующей или подменённой revision UI
показывает текущую редакцию с явным предупреждением; legacy entry без revision
не выдаётся за архивную. Повтор задачи и похожая задача всегда открывают текущий
контент.

### Run

Backend-модель `Run`/`RunItem` реализована. Диагностика и пробник уже имеют
локальные resumable state machines с явным guest/account owner. Вход забирает
guest-run, а logout, смена аккаунта или невалидный owner синхронно удаляют чужой
runtime до сетевой синхронизации; owner-scoped история при этом сохраняется.
Завершённые пробники имеют bounded cross-device archive с batch-загрузкой
run/items/latest attempts. Backend-контракт ADR 0016 хранит versioned mutable
checkpoint отдельно от append-only attempts и удаляет его при submit/abandon.
Диагностика загружает проверенные позиции как append-only attempts, debounce-ит
текущий draft, гидратирует только совместимый актуальный blueprint и при
расхождении предлагает явный выбор без last-write-wins. Пробник синхронизирует
assignment и drafts до общей сдачи, затем сохраняет отдельный слой `AUTO`
проверки и `RUBRIC_SELF` самооценки до единственного `submitRun`. Деталь
отдельной history attempt и результат завершённого пробника выбирают точные
immutable snapshots. Для пробника сервер принимает не более одного кандидата на
позицию, а клиент применяет архивный набор атомарно только после совпадения
content revision, всех task ID и task revisions с owner-scoped hydrated run.
Если хотя бы одна ревизия отсутствует или URL подменён, весь разбор остаётся на
текущем контенте с явным предупреждением; retry всегда использует текущие задачи.

## 8. Зафиксированные продуктовые решения

- MVP доказывает один контур: FTN P1 mathematics.
- Канонический production-контент - на сербском латиницей; UI и проверенные
  переводы доступны на сербском, английском и русском.
- Первый полезный опыт доступен без регистрации.
- Машинная проверка применяется в тренировке и не выдается за официальное
  оценивание хода решения.
- Полный пробник повторяет актуальные 4 часа и не показывает помощь до сдачи.
- План является пересчитываемым списком следующих действий, а не линейным
  курсом и не выдуманной оценкой часов до успеха.
- Конкретный Figma node является обязательной pixel-perfect спецификацией
  экрана, но не источником фактов об экзамене. Экран без Figma может быть только
  явно provisional skeleton до отдельного дизайна.
- Тренажеры P3-P8, AI-чат, занятия с преподавателями, social и paywall не входят
  в ближайший MVP. Несуществующие P2 и экзамен по физике не показываются вообще.
- AI может исследоваться только как дополнительный rubric-review поверх
  заготовленного решения; его результат не называется официальным баллом без
  отдельной калибровки.
- Целевой продуктовый API - GraphQL на `gqlgen`; OAuth redirects, callback,
  logout и health остаются обычными HTTP endpoints в том же Go-монолите.
- Целевой deploy - private Kubernetes: Cloudflare Tunnel напрямую к внутренним
  web/api Services, Kargo -> Git -> ArgoCD, private registry, SealedSecrets и
  Neon Postgres. Публичного app Gateway route и origin DNS быть не должно.

## 9. Открытые решения

Перед реализацией соответствующих срезов нужно отдельно утвердить или
откалибровать:

1. Длительность и состав короткой диагностики на pilot-данных; она не называется
   точной симуляцией P1.
2. Минимальный объем проверенного контента для публичного обещания готовности;
   сейчас verified весь пилотный банк из 30 задач в 10 темах.
3. Pilot-калибровку текущей deterministic mastery и task-of-the-day эвристики.
4. Какие данные о факультетах поддерживаются вручную и как часто
   перепроверяются по официальным источникам.
5. Нужен ли локализованный task Markdown; текущий осознанный контракт -
   канонический сербский контент и локализованный интерфейс.
6. Безопасный analytics image или другой privacy-friendly provider; интеграция
   до этого остается выключенной.

Порядок реализации и критерии готовности вынесены в
`docs/DEVELOPMENT_PLAN.ru.md`.

## 10. Release blockers

1. Выпустить новый Google OAuth client для
   `https://doindeksa.rs/api/v1/auth/google/callback`, запечатать credentials и
   доказать login/logout в production.
2. Влить private GitOps workload, поднять outbound-only Cloudflare Tunnel,
   выполнить Kargo promotion и проверить private routing, health, GraphQL,
   cookie attributes, Neon migration и rollback.
3. Umami не блокирует запуск продукта: runtime config fail-closed. Issue #12
   остается открытым до появления vulnerability-clean image.
4. 30 задач достаточно для portfolio-preview, но не для обещания устойчиво
   разнообразной подготовки; расширение verified-банка остается отдельным
   контентным критическим путем.
